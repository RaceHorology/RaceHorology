<Window x:Class="DSVAlpin2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DSVAlpin2"
        mc:Ignorable="d"
        Title="DSVAlpin2" Height="1024" Width="1400">
  
  <Window.CommandBindings>
    <CommandBinding Command="Open"  Executed="OpenCommandBinding_Executed" />
    <CommandBinding Command="Close" Executed="CloseCommandBinding_Executed" />
  </Window.CommandBindings>
  <Window.InputBindings>
    <KeyBinding Key="O" Modifiers="Control" Command="Open"/>
    <KeyBinding Key="C" Modifiers="Control" Command="Close"/>
  </Window.InputBindings>
  
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition/>
      <RowDefinition Height="14*"/>
    </Grid.RowDefinitions>
    <Menu x:Name="menu" VerticalAlignment="Top" Width="Auto" Margin="0,0,0,0" Grid.Row="0">
      <MenuItem Header="File">
        <MenuItem Header="_Open" Click="MenuOpen_Click"/>
        <MenuItem Header="_Close" Click="MenuClose_Click" />
      </MenuItem>
      <MenuItem Header="Help" />
    </Menu>
    <DataGrid x:Name="dgParticipants" Height="Auto" Margin="10" Width="Auto" Grid.Row="1"/>

  </Grid>
</Window>
