<UserControl x:Class="RaceHorology.StartNumbersUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RaceHorology"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
  <Grid>


    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0">
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="5"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <Grid Grid.Row="0">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dgStartList" Margin="5" Grid.Row="0" AlternationCount="2" AlternatingRowBackground="#FFf8faf7" AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="True">
          <DataGrid.Columns>
            <DataGridTextColumn local:DataGridUtil.Name="StartNumber" Header="Startnummer" Binding="{Binding StartNumber, Mode=TwoWay}" />
            <DataGridTextColumn local:DataGridUtil.Name="Name" Header="Name" Binding="{Binding Participant.Name, Mode=OneWay}" />
            <DataGridTextColumn local:DataGridUtil.Name="Firstname" Header="Vorname" Binding="{Binding Participant.Firstname, Mode=OneWay}" />
            <DataGridTextColumn local:DataGridUtil.Name="Year" Header="Jahrgang" Binding="{Binding Participant.Year, Mode=OneWay}" />
            <DataGridTextColumn local:DataGridUtil.Name="Class" Header="Klasse" Binding="{Binding Participant.Class, Mode=OneWay}" />
            <DataGridTextColumn local:DataGridUtil.Name="Club" Header="Verein" Binding="{Binding Participant.Club, Mode=OneWay}" />
            <DataGridTextColumn local:DataGridUtil.Name="Points" Header="Punkte" Binding="{Binding Participant.Points, Mode=OneWay}" />
          </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="2">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <Button x:Name="btnDeleteAll" Content="Alle Löschen" Margin="5" Padding="5" Grid.Column="0" Click="btnDeleteAll_Click"/>
          <Button x:Name="btnInsert" Content="Einfügen" Margin="5" Padding="5" Grid.Column="2" Click="btnInsert_Click"/>
          <Button x:Name="btnRemove" Content="Entfernen" Margin="5" Padding="5" Grid.Column="3" Click="btnRemove_Click"/>
        </Grid>

      </Grid>

      <GridSplitter x:Name="gridSplitter2" HorizontalAlignment="Stretch" Height="5" Grid.Row="1" />

      <Grid Grid.Row="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="5"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Verlosen" Grid.Column="0">

          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Content="Gruppierung:" Margin="5" Grid.Row="0" Grid.Column="0"/>
            <ComboBox x:Name="cmbGrouping" Margin="5" Grid.Column="1" />

            <Label Content="Verlosungszahl:" Margin="5" Grid.Row="1" Grid.Column="0"/>
            <TextBox x:Name="txtVerlosung" Margin="5" Grid.Row="1" Grid.Column="1"/>

            <Label Content="Richtung:" Margin="5" Grid.Row="2" Grid.Column="0"/>
            <ComboBox x:Name="cmbDirection" Margin="5" Grid.Row="2" Grid.Column="1"/>

            <Label Content="Nächste Startnummer:" Margin="5" Grid.Row="3" Grid.Column="0"/>
            <TextBox x:Name="txtNextStartNumber" Margin="5" Grid.Row="3" Grid.Column="1"/>

            <Button Content="Vergeben" Margin="5" Padding="5" Grid.Row="4" Grid.Column="1" />
          </Grid>

        </GroupBox>

        <GridSplitter x:Name="gridSplitter3" VerticalAlignment="Stretch" Width="5" Grid.Column="1" />

        <GroupBox Header="Manuell" Grid.Column="2">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="*"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DataGrid x:Name="dgParticipants" Margin="5" Grid.Column="1" Grid.Row="0" AlternationCount="2" AlternatingRowBackground="#FFf8faf7" AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="True">
              <DataGrid.Columns>
                <DataGridTextColumn local:DataGridUtil.Name="Name" Header="Name" Binding="{Binding Name, Mode=OneWay}" />
                <DataGridTextColumn local:DataGridUtil.Name="Firstname" Header="Vorname" Binding="{Binding Firstname, Mode=OneWay}" />
                <DataGridTextColumn local:DataGridUtil.Name="Year" Header="Jahrgang" Binding="{Binding Year, Mode=OneWay}" />
                <DataGridTextColumn local:DataGridUtil.Name="Class" Header="Klasse" Binding="{Binding Class, Mode=OneWay}" />
                <DataGridTextColumn local:DataGridUtil.Name="Club" Header="Verein" Binding="{Binding Club, Mode=OneWay}" />
                <DataGridTextColumn local:DataGridUtil.Name="Points" Header="Punkte" Binding="{Binding Points, Mode=OneWay}" />
              </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Row="4" Grid.Column="1">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>

              <Label Content="Startnummer:" Grid.Column="0" Margin="5" />
              <TextBox x:Name="txtNextStartNumberManual" Grid.Column="1" Margin="5" Width="40"/>

              <Button x:Name="btnAssign" Content="Zuweisen" Margin="5" Padding="5" Grid.Column="3" Click="btnAssign_Click"/>
            </Grid>
          </Grid>          
        </GroupBox>

      </Grid>

      <StackPanel Orientation="Horizontal"  Grid.Row="4" Grid.Column="1" HorizontalAlignment="Right" >
        <Button x:Name="btnReset" Content="Zurücksetzen" Margin="5" Padding="5" Click="BtnReset_Click"/>
        <Button x:Name="btnApply" Content="Speichern" Margin="5" Padding="5" Click="BtnApply_Click"/>
      </StackPanel>

    </Grid>
  </Grid>
</UserControl>
